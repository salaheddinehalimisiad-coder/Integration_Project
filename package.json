{
  "name": "datamediator-root",
  "version": "3.2.0",
  "description": "DataMediator — Plateforme de médiation de données GAV/LAV",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "python -m uvicorn main:app --host 0.0.0.0 --port 5001 --reload",
    "dev": "npm run frontend",
    "setup": "python sources/setup_enterprise_sources.py && cd frontend && npm install"
  }
}
